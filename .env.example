NODE_ENV=development
PORT=3333

# Banco (Docker Compose — host 5435)
DATABASE_URL="postgresql://mensalhub:mensalhub@localhost:5435/mensalhub?schema=public"

# JWT — gerar com: openssl rand -hex 32
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# CORS — origem do front
CORS_ORIGIN=http://localhost:3000
